apiVersion: v1
kind: Service
metadata:
  name: {{ include "websuite.fullname" . }}
  labels:
    {{- include "websuite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "websuite.selectorLabels" . | nindent 4 }}
